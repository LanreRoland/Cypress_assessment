name: Cypress

on: [push]

jobs: 
  build:
  runs-on: ubuntu latest
  steps:
  -name: Checkout
   uses: actions/checkout@v3
  -name: Run Cypress Test
   users: cypress-io/github-acions@v5
   with:
   node-version: 16
